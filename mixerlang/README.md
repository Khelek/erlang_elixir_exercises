# Run erlang from elixir, and elixir from erlang

Warning: in Makefile setted elixir ebins. You may need change on own.

```make elixir``` and run `:puts.print("elixir")`
```make erlang``` and run `puts:elixir().`
